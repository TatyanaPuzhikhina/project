#include  <stdio.h>
int main () {
int d = 0;
int i = 1;
int numbr = 5;
while (i <= numbr)
 {
  if (numbr % i++ == 0)
   { 
     d++;
   }
 }
    if (d==2) {
    printf ("Ваше число простое");
    }
    else {
    printf ("Ваше число не является простым числом");
    }
    return 0;
}
